/* Phase 3 replaces this stub with the lazy poster-facade iframe embed. */
export default function OfficeDemo() {
  return (
    <div className="flex h-full items-center justify-center font-mono text-xs tracking-[0.15em] text-neutral-500">
      ▶ THE OFFICE BOOTS IN PHASE 3
    </div>
  );
}
